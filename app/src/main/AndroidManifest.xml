<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GiggleMusicPiano"
        android:usesCleartextTraffic="true"
        tools:replace="android:theme">
        <activity
            android:name=".fragments.stream.views.WebActivity"
            android:exported="false" />
        <activity android:name=".activities.SettingsActivity" />
        <activity
            android:name=".gallery.PhotoActivity"
            android:exported="false"
            android:parentActivityName=".activities.HomeActivity"
            android:theme="@style/Theme.GiggleMusicPiano.NoActionBar" />
        <activity
            android:name=".videos.VideoDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".videos.VideoListActivity"
            android:exported="false"
            android:parentActivityName=".activities.HomeActivity"
            android:theme="@style/Theme.GiggleMusicPiano.NoActionBar" />
        <activity
            android:name=".activities.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.GiggleMusicPiano" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GiggleMusicPiano.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".fragments.stream.notification.Notification"
            android:enabled="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>